#include <iostream>
#include <vector>
#include <queue>

using namespace std;

int vertices, edges;
vector<vector<int>> graph(10);
vector<int> visited(10);
vector<int> bfs;
vector<int> dfs;


int main() {

    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    

}